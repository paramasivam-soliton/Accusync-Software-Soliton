<!--
    Copyright (c) 2026 Natus Sensory. All rights reserved.
-->

<Window x:Class="AccuSync.WPF.Views.Login.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:AccuSync.WPF.Resources"
        Title="{x:Static res:Strings.LoginWindow_Title}"
        Height="550" 
        Width="450"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <!-- Modern ComboBox Style -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="BorderBrush" Value="#428FEC"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Height" Value="48"/>
        </Style>

        <!-- Modern TextBox Style for Password -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="BorderBrush" Value="#428FEC"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="CaretBrush" Value="#428FEC"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Modern PasswordBox Style -->
        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="BorderBrush" Value="#428FEC"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="CaretBrush" Value="#428FEC"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Show/Hide Password Button Style -->
        <Style x:Key="PasswordToggleButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E8F4FD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Button Style -->
        <Style x:Key="SignInButton" TargetType="Button">
            <Setter Property="Background" Value="#428FEC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="25"
                                Padding="20,0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5BA3F5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#357ABD"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#B0BEC5"/>
                                <Setter Property="Foreground" Value="#ECEFF1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#D4E9F9" 
            BorderBrush="#428FEC" 
            BorderThickness="2"
            CornerRadius="20">
        <Border.Effect>
            <DropShadowEffect Color="#428FEC" 
                            Direction="0" 
                            ShadowDepth="0" 
                            BlurRadius="40" 
                            Opacity="0.3"/>
        </Border.Effect>

        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center">
                <TextBlock Text="{x:Static res:Strings.LoginWindow_AppName}"
                          FontSize="42"
                          FontWeight="Bold"
                          Foreground="#428FEC"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,5"/>
                <TextBlock Text="{x:Static res:Strings.LoginWindow_Subtitle}"
                          FontSize="13"
                          Foreground="#5A6C7D"
                          HorizontalAlignment="Center"
                          FontWeight="Medium"/>
            </StackPanel>

            <!-- Error Message -->
            <TextBlock Grid.Row="2"
                      Text="{Binding ErrorMessage}"
                      Foreground="#E74C3C"
                      FontSize="12"
                      TextWrapping="Wrap"
                      HorizontalAlignment="Center"
                      FontWeight="Medium"
                      Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

            <!-- Username -->
            <StackPanel Grid.Row="4">
                <TextBlock Text="{x:Static res:Strings.LoginWindow_Username}"
                          FontSize="13"
                          Foreground="#2C3E50"
                          Margin="5,0,0,8"
                          FontWeight="SemiBold"/>
                <ComboBox ItemsSource="{Binding Usernames}"
                         SelectedItem="{Binding SelectedUsername}"
                         Style="{StaticResource ModernComboBox}"/>
            </StackPanel>

            <!-- Password -->
            <StackPanel Grid.Row="6">
                <TextBlock Text="{x:Static res:Strings.LoginWindow_Password}"
                          FontSize="13"
                          Foreground="#2C3E50"
                          Margin="5,0,0,8"
                          FontWeight="SemiBold"/>
                <Grid>
                    <!-- PasswordBox (hidden when showing password) -->
                    <PasswordBox x:Name="PasswordBox"
                                Style="{StaticResource ModernPasswordBox}"
                                PasswordChanged="PasswordBox_PasswordChanged"/>

                    <!-- TextBox (shown when revealing password) -->
                    <TextBox x:Name="PasswordTextBox"
                            Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"
                            Style="{StaticResource ModernTextBox}"
                            Visibility="Collapsed"/>

                    <!-- Show/Hide Password Button -->
                    <Button x:Name="TogglePasswordButton"
                           Style="{StaticResource PasswordToggleButton}"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Margin="0,0,5,0"
                           Click="TogglePasswordVisibility_Click"
                           ToolTip="{x:Static res:Strings.LoginWindow_ShowPassword}">
                        <Path x:Name="EyeIcon"
                              Width="20" 
                              Height="20"
                              Stretch="Uniform"
                              Fill="#428FEC"
                              Data="M12 4.5C7 4.5 2.73 7.61 1 12c1.73 4.39 6 7.5 11 7.5s9.27-3.11 11-7.5c-1.73-4.39-6-7.5-11-7.5zM12 17c-2.76 0-5-2.24-5-5s2.24-5 5-5 5 2.24 5 5-2.24 5-5 5zm0-8c-1.66 0-3 1.34-3 3s1.34 3 3 3 3-1.34 3-3-1.34-3-3-3z"/>
                    </Button>
                </Grid>
            </StackPanel>

            <!-- Sign In Button -->
            <Button Grid.Row="8"
                   Content="{x:Static res:Strings.LoginWindow_SignIn}"
                   Command="{Binding SignInCommand}"
                   Style="{StaticResource SignInButton}"
                   IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>

            <!-- Loading Indicator -->
            <StackPanel Grid.Row="9" 
                       VerticalAlignment="Bottom"
                       HorizontalAlignment="Center"
                       Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="{x:Static res:Strings.LoginWindow_Authenticating}"
                          FontSize="12"
                          Foreground="#5A6C7D"
                          HorizontalAlignment="Center"
                          Margin="0,20,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>