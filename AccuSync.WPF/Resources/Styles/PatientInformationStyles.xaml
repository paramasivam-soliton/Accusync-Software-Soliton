<!--
    Copyright (c) 2026 Natus Sensory. All rights reserved.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:AccuSync.WPF.Converters">

    <!-- PatientInformationStyles.xaml
         Shared styles for the PatientInformationView shell and its three tab UserControls.
         Merged in App.xaml alongside RibbonToolbarStyles. -->

    <!-- Needed by ValidatedTextBox to drive the green "valid + non-empty" border. -->
    <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>

    <!-- Color Brushes -->
    <!-- TODO: Consolidate with any app-wide brush definitions to avoid
         duplicate or conflicting color keys across resource dictionaries. -->

    <SolidColorBrush x:Key="NavyBrush"       Color="#003049"/>
    <SolidColorBrush x:Key="LightBlueBrush"  Color="#A3C4D9"/>
    <SolidColorBrush x:Key="GrayBrush"       Color="#8B9BA8"/>
    <SolidColorBrush x:Key="BrightBlueBrush" Color="#0077C8"/>
    <SolidColorBrush x:Key="TealBrush"       Color="#00BFA5"/>
    <SolidColorBrush x:Key="CyanBrush"       Color="#00A99D"/>
    <SolidColorBrush x:Key="BorderBrush"     Color="#e5e5e5"/>
    <SolidColorBrush x:Key="LightGrayBg"     Color="#f8f9fa"/>

    <LinearGradientBrush x:Key="CharcoalHeaderBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#3A3A3C" Offset="0"/>
        <GradientStop Color="#202124" Offset="1"/>
    </LinearGradientBrush>

    <!-- Form Field Styles -->

    <!-- Base text input. Extended by ValidatedTextBox for error/success borders. -->
    <Style x:Key="FieldTextBox" TargetType="TextBox">
        <Setter Property="Padding" Value="8,8"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="BorderBrush" Value="#dddddd"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="White"/>
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource BrightBlueBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Fully custom ComboBox chrome with rounded corners and shadow popup.
         NOTE: The inner ToggleButton template is deeply nested — consider
         extracting it into its own keyed style if this gets harder to maintain. -->
    <Style x:Key="FieldComboBox" TargetType="ComboBox">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="Foreground" Value="#5C5149"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton"
                                      Focusable="False"
                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press">
                            <ToggleButton.Style>
                                <Style TargetType="ToggleButton">
                                    <Setter Property="Background" Value="White"/>
                                    <Setter Property="BorderBrush" Value="#dddddd"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="6">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <ContentPresenter Grid.Column="0" Margin="10,6"/>
                                                        <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z" Fill="#666"
                                                              Margin="0,0,10,0" VerticalAlignment="Center"/>
                                                    </Grid>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="#002856"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ToggleButton.Style>
                        </ToggleButton>
                        <ContentPresenter Name="ContentSite" IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          Margin="12,6,32,6" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        <Popup Name="Popup" Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                            <Border CornerRadius="6" MaxHeight="200"
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    Background="White" BorderThickness="1" BorderBrush="#D1D5DB">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" BlurRadius="8" Opacity="0.15"/>
                                </Border.Effect>
                                <ScrollViewer Margin="4,6" SnapsToDevicePixels="True">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- "✕" button overlaid inside a TextBox for one-click clearing. -->
    <Style x:Key="InnerClearButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="#999"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="Width" Value="24"/>
        <Setter Property="Height" Value="24"/>
        <Setter Property="Content" Value="✕"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="#666"/>
                <Setter Property="Background" Value="#f0f0f0"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SectionCard" TargetType="Border">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0,0,0,24"/>
    </Style>

    <!-- Validation Styles -->

    <ControlTemplate x:Key="ValidationErrorTemplate">
        <StackPanel>
            <Border BorderBrush="#dc3545" BorderThickness="2" CornerRadius="2">
                <AdornedElementPlaceholder x:Name="adorner"/>
            </Border>
            <TextBlock Text="{Binding ElementName=adorner, Path=AdornedElement.(Validation.Errors)[0].ErrorContent}"
                       Foreground="#dc3545" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
        </StackPanel>
    </ControlTemplate>

    <!-- Three-state visual feedback: red (error) → blue (focused) → green (valid + non-empty).
         Trigger order matters here — later triggers win, so focus blue intentionally
         overrides the green "all good" state. -->
    <Style x:Key="ValidatedTextBox" TargetType="TextBox" BasedOn="{StaticResource FieldTextBox}">
        <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ValidationErrorTemplate}"/>
        <Style.Triggers>
            <Trigger Property="Validation.HasError" Value="True">
                <Setter Property="BorderBrush" Value="#dc3545"/>
                <Setter Property="BorderThickness" Value="2"/>
                <Setter Property="ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
            </Trigger>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource BrightBlueBrush}"/>
            </Trigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.HasError)}" Value="False"/>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Text, Converter={StaticResource StringToVisibilityConverter}}" Value="Visible"/>
                </MultiDataTrigger.Conditions>
                <Setter Property="BorderBrush" Value="#28a745"/>
                <Setter Property="BorderThickness" Value="1"/>
            </MultiDataTrigger>
        </Style.Triggers>
    </Style>

    <!-- TODO: This style is not referenced anywhere. Remove if not needed. -->
    <Style x:Key="WarningBadge" TargetType="Border">
        <Setter Property="Background" Value="#dc3545"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="6,2"/>
        <Setter Property="Margin" Value="6,0,0,0"/>
    </Style>

    <!-- Unified CheckBox Style -->

    <!-- Implicit style — affects every CheckBox that merges this dictionary.
         Use Style="{x:Null}" on any CheckBox that needs default WPF chrome. -->
    <Style TargetType="CheckBox">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border x:Name="CheckBorder"
                            Width="16" Height="16"
                            BorderBrush="#D1D5DB" BorderThickness="1.5"
                            CornerRadius="3" Background="White"
                            Margin="0,0,6,0" VerticalAlignment="Center">
                            <Path x:Name="CheckMark"
                              Width="10" Height="10"
                              Stretch="Uniform"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Data="M 2 8 L 6 12 L 14 4"
                              Stroke="White" StrokeThickness="2"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              StrokeLineJoin="Round"
                              Visibility="Collapsed"/>
                        </Border>
                        <ContentPresenter VerticalAlignment="Center"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckBorder" Property="Background" Value="#005FBE"/>
                            <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#005FBE"/>
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="CheckMark" Property="Data" Value="M 2 8 L 6 12 L 14 4"/>
                        </Trigger>
                        <!-- Indeterminate: shows a dash instead of a checkmark -->
                        <Trigger Property="IsChecked" Value="{x:Null}">
                            <Setter TargetName="CheckBorder" Property="Background" Value="#005FBE"/>
                            <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#005FBE"/>
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="CheckMark" Property="Data" Value="M 3 8 L 13 8"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#005FBE"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- DatePicker Calendar Size -->

    <!-- Scaled to 140% because the default WPF calendar is tiny on high-DPI screens. -->
    <Style TargetType="DatePicker">
        <Setter Property="CalendarStyle">
            <Setter.Value>
                <Style TargetType="Calendar">
                    <Setter Property="LayoutTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="1.4" ScaleY="1.4"/>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>