<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>annotations</Nullable>
    <AssemblyName>AccuSync.Application</AssemblyName>
    <RootNamespace>AccuSync.Application</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Needs User/PatientData/TestRecord and the repository/service abstractions. -->
    <ProjectReference Include="..\AccuSync.Core\AccuSync.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Standalone reversible encryption for usernames (EncryptionService) — no web
         server/hosting involved, just the Data Protection crypto/key-management library. -->
    <PackageReference Include="Microsoft.AspNetCore.DataProtection" />

    <!-- Reserved for the future AccuSync.DeviceCommunication extraction —
         not yet used by any code in this project. -->
    <PackageReference Include="System.IO.Ports" />
  </ItemGroup>

  <ItemGroup>
    <Compile Update="Resources\Strings.Designer.cs">
      <DesignTime>True</DesignTime>
      <AutoGen>True</AutoGen>
      <DependentUpon>Strings.resx</DependentUpon>
    </Compile>
  </ItemGroup>

  <ItemGroup>
    <EmbeddedResource Update="Resources\Strings.resx">
      <Generator>PublicResXFileCodeGenerator</Generator>
      <LastGenOutput>Strings.Designer.cs</LastGenOutput>
    </EmbeddedResource>
  </ItemGroup>

</Project>
